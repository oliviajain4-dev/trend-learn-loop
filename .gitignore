# Python
.venv/
venv/
__pycache__/
*.pyc

# 환경변수 (API 키 - Gemini, Claude 등 모든 키)
.env
.env.*
.env.local

# IDE
.vscode/
.idea/

# 결과물
output/
*.log

# 수집 원자료(디버그용, 휘발성 — 최종 브리핑 data/briefs 와 다름)
data/collected/

# Memory 런타임 상태(seen 로그 등, 휘발성) — .gitkeep 만 추적
data/memory/*.json

# 대시보드 산출물·저장 교과서(런타임 생성)
data/*.html
data/textbooks/

# 파이썬 패키징/도구 캐시
*.egg-info/
.ruff_cache/
.pytest_cache/

# Streamlit (로컬 시크릿은 절대 커밋 금지)
.streamlit/secrets.toml
