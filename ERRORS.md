# ERRORS — AI가 틀린 순간과 수정 내역 (날것)

> 이 파일은 이 과제의 **핵심 증거물**이다. 구현 중 AI(코워크 자신 포함)가
> 프로젝트와 직접 관련해서 틀렸거나 엉뚱하게 한 순간, 그리고 어떻게 바로잡았는지를
> 다듬지 말고 그대로 남긴다.
>
> 형식: `## 날짜 · #번호 — 한 줄 제목` / **무엇** / **왜(추정)** / **고침**
> 원칙: 억지로 채우지 않는다. 실제로 발생한 프로젝트 관련 오류만 기록한다.

---

## 2026-07-10 · #1 — 미사용 import 를 커밋 직전 ruff 가 잡음
- **무엇**: `app.py` 에서 `load_all_briefs` 를 import 했으나, 실제로는 `_load_briefs`
  내부에서 `load_brief` 를 직접 호출하도록 바꿔 죽은 import 가 남음.
- **왜**: 로더를 `@st.cache_data` 함수로 감싸며 상단 import 를 정리하지 않음.
- **고침**: `ruff check src/` 가 F401 로 지적 → import 제거. 검증 도구가 실제로 잡은 첫 사례.

## 2026-07-10 · #2 — 지원종료된 Streamlit 인자(use_container_width) 사용
- **무엇**: 카드 버튼·게이지·표에 `use_container_width=True` 를 씀. 이 인자는
  2025-12-31 지원종료 예정(오늘은 이미 그 이후)이라 곧 제거됨.
- **왜**: 예전부터 흔히 쓰던 관용을 그대로 사용 — 최신 Streamlit 변경사항 미반영.
- **고침**: 앱을 실제 실행 + AppTest 로 렌더하자 deprecation 경고가 떠서 발견 →
  `width="stretch"` 로 전량 교체. "임포트만 확인"으로는 못 잡고, **실제 렌더까지
  돌려야 잡히는** 부류였음(검증을 실행 단계까지 밀어붙인 이유).

## 2026-07-10 · #3 — Gemini 기본 모델명을 훈련 기억대로 넣었다가 404
- **무엇**: GeminiProvider 기본 모델을 `gemini-2.5-flash` 로 지정. 그럴듯했지만
  이 키(무료 티어)에서 **generateContent 가 404 NOT_FOUND** 로 거부.
- **왜**: "요즘 gemini flash면 2.5-flash지" 하는 훈련 기억(추측)으로 모델명을 박음.
  `models.list()` 엔 이름이 보였지만 실제 generate 는 안 됐음(리스트≠호출가능).
- **고침**: 커밋 전에 **실호출 스윕**을 돌려 실제 되는 모델을 찾음 →
  `gemini-2.5-flash`/`-lite`=404, `gemini-2.0-flash*`=429(쿼터), **`gemini-flash-lite-latest`=그린패스**.
  기본값을 실동작 확인된 것으로 교체. "추측 금지·실호출 먼저"가 커밋 전에 잡은 사례.

## 2026-07-10 · #4 — 지표(precision) 계산이 '짧은' 유령인용을 놓침
- **무엇**: compute_metrics 에서 사실문 필터(≥20자)를 정밀도 계산에도 적용 →
  주입한 유령인용 문장이 13자라 필터에서 빠져 **citation_precision 이 1.0 그대로**(하락 안 함).
- **왜**: 커버리지용 길이필터를 정밀도에도 그대로 재사용. 유령인용은 길이 무관인데.
- **고침**: 정밀도는 '인용 달린 모든 문장' 기준으로 분리(길이필터 제외) →
  주입 후 precision 0.875 로 정확히 하락. **환각주입 테스트가 잡은 지표 버그.**


## 2026-07-10 · #5 — v3 문서에서 Scout 를 '에이전트'로 잘못 표기
- **무엇**: 기획서 v3 §3 표에서 Scout(정찰)의 종류를 `[에이전트]`로 적었다.
- **왜**: 에이전트 작동 순서를 짜며 "발견도 능동적이니 에이전트"라고 뭉뚱그림. 실제 구현해 보니
  '훑어서 새 것만'은 폴링+집합차 = **순수 결정론**이고, 능동적 판단('교과서 감이냐')은 다음 단계
  **Triage** 다.
- **고침**: Scout 종류를 `[결정론(발견·신규성)]`으로 정정, 판단은 Triage(에이전트)로 분리 명기.
  "검증기가 환각하면 안 되듯, '무엇이 에이전트인가'도 과장하지 않는다." 구현이 문서의 과장을 잡은 사례.
� 에이전트인가'도
  과장하지 않는다." 구현이 문서의 과장을 잡은 사례.
